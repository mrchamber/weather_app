import { WEATHER_API_URL, WEATHER_API_KEY } from '../../api';
import React, { useState } from 'react';
import './header.css'
const Header = () => {
    return (
        <header>
            <div>
                <h2>Mary's Weather App</h2>
            </div>
        </header>
    )
}

export default Header;